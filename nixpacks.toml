[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "ffmpeg", "postgresql"]

[phases.install]
cmds = ["pip3.11 install --break-system-packages -r requirements.txt"]

[start]
cmd = "python3.11 bot.py"
